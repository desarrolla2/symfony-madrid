<div>
    <ul>
    {% for thread in feeds %}
        <li>
            <p>
                <a href="{{ thread.link }}" target="_blank">{{ thread.title }}</a> 
                {{ thread.description | striptags | truncate(160)  }} <small> el {{ thread.pubDate | date('d/m/Y', 'Europe/Madrid')}}</small>
                    
        </li>
    {%endfor%}
    </ul>
</div>
